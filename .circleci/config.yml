version: 2.1

workflows:
  jobs:
    jobs:
      - test:
          resource_class: small
          context: test
          docker:
            - image: cimg/aws:2023.06
          steps:
            - checkout
            - run: aws --version
            - run: npm --version
            - run: env
            - run: aws sts get-caller-identity
